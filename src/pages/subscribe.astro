---
export const prerender = true;
import { getCollection } from "astro:content";
import Layout from "@layouts/Layout.astro";
import Header from "@components/Header.astro";
import Footer from "@components/Footer.astro";

const errors = { username: "", email: "", password: "" };
if (Astro.request.method === "POST") {
  try {
    const data = await Astro.request.formData();
    const email = data.get("email");
    // Do something with the data
  } catch (error) {
    if (error instanceof Error) {
      console.error(error.message);
    }
  }
}
---

<Layout />
<Header />
<!-- <div class="mt-12 md:mt-32 lg:mt-12 w-50 lg:w-2/5 m-auto flex flex-col items-center bg-white p-10" name="contact" >
      <h3 class="text-blue-900 font-semibold text-center text-3xl">Subscribe!</h3>
      <p class="mt-6 text-gray-600 text-lg text-center leading-normal font-light">Get the latest updates from the blog by joining our newsletter.
          <br>Monthly updates guaranteed.
      </p>
      <div class="mt-6 flex flex-col md:flex-row w-full md:px-8">
          <input class="flex flex-grow px-4 py-2 rounded text-gray-500 border border-gray-500" placeholder="Email Address">
          <button class="mt-2 md:mt-0 md:ml-2 bg-blue-500 shadow-lg rounded text-white px-4 py-3">Subscribe!</button>
      </div>
      <p class="mt-6 text-gray-500">Check out my Medium if you haven't:<a class="ml-2 text-blue-500" href="https://medium.com/@cheesyminecart">Medium</a></p>
    </div> -->

<form
  name="contact"
  method="POST"
  data-netlify="true"
  class="mt-12 md:mt-32 lg:mt-12 w-50 lg:w-2/5 m-auto flex flex-col items-center bg-white p-10"
  netlify-honeypot="bot-field"
>
  <h3 class="text-blue-900 font-semibold text-center text-3xl">Subscribe!</h3>
  <p class="mt-6 text-gray-600 text-lg text-center leading-normal font-light">
    Get the latest updates from the blog by joining our newsletter.
    <br />Monthly updates guaranteed.
  </p>
  <p class="hidden">
    <label>
      maxwell, a cat of many regards: <input name="bot-field" />
    </label>
  </p>
  <div class="mt-6 flex flex-col md:flex-row w-full md:px-8">
    <input
      class="flex flex-grow px-4 py-2 rounded text-gray-500 border border-gray-500"
      placeholder="Email Address"
      type="email"
      name="email"
    />
    <button
      class="mt-2 md:mt-0 md:ml-2 bg-blue-500 shadow-lg rounded text-white px-4 py-3"
      type="submit">Subscribe!</button
    >
  </div>
</form>

<Footer />

<style>
  #hero {
    @apply pb-6 pt-8;
  }
  #hero h1 {
    @apply my-4 inline-block font-bold sm:my-8 sm:text-5xl pt-24 underline;
  }
  #hero .rss-link {
    @apply mb-6;
  }
  #hero .rss-icon {
    @apply mb-2 h-6 w-6 scale-110 fill-skin-accent sm:mb-3 sm:scale-125;
  }
  #hero p {
    @apply my-2 p-2;
  }
  #hero h2 {
    @apply text-2xl font-semibold tracking-wide;
  }

  #regPara {
    @apply italic opacity-60;
  }

  #subButton {
    @apply box-border h-10 w-24 text-center rounded;
  }

  #link-enter {
    @apply text-2xl font-bold;
  }

  #input-box {
    @apply rounded-lg p-4 max-w-fit text-right;
  }
  # .social-wrapper {
    @apply mt-4 flex flex-col sm:flex-row sm:items-center;
  }
  .social-links {
    @apply mb-1 mr-2 whitespace-nowrap sm:mb-0;
  }
</style>
